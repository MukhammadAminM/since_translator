# Since Translator

Веб-приложение для перевода текстов и файлов с русского, арабского и китайского на английский с результатом в формате `.docx`.

## 🚀 Технологии

### Frontend
- **React 18** + **TypeScript**
- **Vite** (сборщик)
- **Tailwind CSS** (стилизация)

### Backend
- **FastAPI** (Python веб-фреймворк)
- **python-docx** (генерация .docx файлов)
- **PyPDF2** (извлечение текста из PDF)

## 📁 Структура проекта

```
since_translator/
├── src/                    # Frontend (React)
│   ├── App.tsx            # Основной компонент
│   ├── main.tsx           # Точка входа
│   └── index.css          # Стили
├── backend/                # Backend (FastAPI)
│   ├── main.py            # FastAPI приложение
│   ├── services/
│   │   ├── translator.py  # Сервис перевода
│   │   └── docx_generator.py  # Генератор .docx
│   └── requirements.txt   # Python зависимости
└── package.json           # Node.js зависимости
```

## 🛠️ Установка и запуск

### 1. Frontend

```bash
# Установка зависимостей
npm install

# Запуск dev сервера (обычно на http://localhost:5173)
npm run dev

# Сборка для production
npm run build
```

### 2. Backend

```bash
# Перейти в директорию backend
cd backend

# Создать виртуальное окружение
python -m venv venv

# Активировать виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Запустить сервер (на http://localhost:8000)
python run.py
# или
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 3. Проверка работы

1. Запустите backend на `http://localhost:8000`
2. Запустите frontend на `http://localhost:5173`
3. Откройте браузер и перейдите на `http://localhost:5173`
4. API документация доступна на `http://localhost:8000/docs`

## 🔌 API Endpoints

### POST `/api/translate`
Переводит текст на английский.

**Request:**
```json
{
  "sourceLang": "ru",
  "text": "Текст для перевода",
  "model": "general"
}
```

**Response:**
```json
{
  "downloadUrl": "/api/download/filename.docx",
  "message": "Перевод выполнен успешно. Модель: general"
}
```

### POST `/api/translate-file`
Переводит содержимое файла на английский.

**Form Data:**
- `file`: файл (PDF, DOC, DOCX, TXT)
- `sourceLang`: "ru" | "ar" | "zh"
- `model`: "general" | "engineering" | "academic" | "scientific"

### GET `/api/download/{filename}`
Скачивание сгенерированного .docx файла.

## 🤖 Интеграция с LLM

✅ **OpenAI уже подключен и настроен!**

Перевод работает через OpenAI API с использованием следующих моделей:
- **general**: `gpt-4o-mini` - быстрая и экономичная модель для обычных переводов
- **engineering**: `gpt-4o` - мощная модель для технических и инженерных текстов
- **academic**: `gpt-4o` - для академических текстов, статей, эссе
- **scientific**: `gpt-4o` - для научных текстов, терминов, отчётов

API ключ настроен в файле `backend/.env`. При необходимости вы можете изменить его.

Для использования других LLM провайдеров смотрите примеры в `backend/services/llm_integration_example.py`

## 📝 Поддерживаемые форматы

- **Текст**: прямой ввод в текстовое поле
- **Файлы**: PDF, DOC, DOCX, TXT

## 🎯 Модели перевода

- **general** - обычный перевод, смешанная тематика
- **engineering** - техническая и инженерная лексика
- **academic** - академические тексты, статьи, эссе
- **scientific** - научные тексты, термины, отчёты

## 🌐 Поддерживаемые языки

- Русский (ru)
- Арабский (ar)
- Китайский (zh)

Все переводы выполняются на английский (en).

## 📦 Зависимости

### Frontend
- react ^18.2.0
- react-dom ^18.2.0
- vite ^5.0.0
- tailwindcss ^3.4.0

### Backend
- fastapi ^0.104.1
- uvicorn ^0.24.0
- python-docx ^1.1.0
- PyPDF2 ^3.0.1
- pydantic ^2.5.0

## 🔧 Настройка

### Изменение порта backend

В `backend/run.py` измените:
```python
uvicorn.run(..., port=8000)  # измените на нужный порт
```

### Изменение URL API в frontend

В `src/App.tsx` измените:
```typescript
const API_BASE_URL = "http://localhost:8000";  // ваш URL backend
```

## 📄 Лицензия

Проект создан для демонстрационных целей.

## 🤝 Вклад

Приветствуются улучшения и предложения!

