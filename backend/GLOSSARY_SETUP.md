# Настройка глоссария

## ✅ Что было сделано

1. **Создан парсер глоссария** (`services/glossary_parser.py`)
   - Поддержка TXT, PDF, DOCX, XLSX файлов
   - Автоматическое извлечение терминов и аббревиатур
   - Создание JSON структуры

2. **Создан менеджер глоссария** (`services/glossary_manager.py`)
   - Загрузка и кэширование глоссариев
   - Форматирование для промптов AI
   - Поиск терминов в тексте

3. **Интегрирован в сервис перевода** (`services/translator.py`)
   - Автоматическая загрузка при старте
   - Использование в промптах OpenAI
   - Поддержка всех языков (ru, ar, zh)

## 🚀 Быстрый старт

### 1. Установите зависимости

```bash
cd backend
pip install -r requirements.txt
```

### 2. Постройте JSON глоссарии

```bash
python build_glossary.py
```

Это создаст файлы:
- `glossary/glossary_russian_to_en.json` ✅ (уже создан - 298 терминов)
- `glossary/glossary_arabic_to_en.json` (требует обработки PDF)
- `glossary/glossary_chinise_to_en.json` (требует обработки PDF)

### 3. Запустите сервер

```bash
python run.py
```

Глоссарий автоматически загрузится при старте!

## 📝 Формат глоссария

### Исходные файлы

Разместите файлы в соответствующих папках:
- `glossary/russian/` - русские термины
- `glossary/arabic/` - арабские термины  
- `glossary/chinise/` - китайские термины

### Поддерживаемые форматы

**TXT:**
```
Ракета-носитель – Launch vehicle
Универсальный ракетный модуль (УРМ) – Universal rocket module (URM)
```

**PDF/DOCX:** Аналогичный формат

**XLSX:** 
- Колонка A: исходный термин
- Колонка B: перевод

### JSON структура

```json
{
  "ракета-носитель": {
    "source": "Ракета-носитель",
    "target": "Launch vehicle",
    "source_abbr": null,
    "target_abbr": null
  },
  "урм": {
    "source": "Универсальный ракетный модуль (УРМ)",
    "target": "Universal rocket module (URM)",
    "source_abbr": "УРМ",
    "target_abbr": "URM"
  }
}
```

## 🔧 Как это работает

1. При старте сервера загружаются все доступные JSON глоссарии
2. При переводе текста глоссарий автоматически включается в промпт для AI
3. AI модель использует точные переводы из глоссария для специализированных терминов
4. Максимум 100 терминов включается в каждый промпт (для экономии токенов)

## 📊 Текущий статус

- ✅ Русский глоссарий: **298 терминов** (из words.txt)
- ⚠️ Арабский глоссарий: требует обработки PDF файлов
- ⚠️ Китайский глоссарий: требует обработки PDF файлов

**Примечание:** PDF файлы требуют установленных библиотек PyPDF2 и python-docx. 
Они уже в requirements.txt, просто установите зависимости.

## 🔄 Обновление глоссария

1. Добавьте/измените файлы в папках `glossary/{language}/`
2. Запустите `python build_glossary.py`
3. Перезапустите сервер

## 💡 Примеры использования

Глоссарий автоматически используется при переводе. Например:

**Входной текст (русский):**
```
Ракета-носитель с универсальным ракетным модулем УРМ-1 запущена на орбиту.
```

**Перевод с глоссарием:**
```
Launch vehicle with universal rocket module URM-1 launched into orbit.
```

Термины "Ракета-носитель", "Универсальный ракетный модуль" и "УРМ-1" будут переведены точно согласно глоссарию.


