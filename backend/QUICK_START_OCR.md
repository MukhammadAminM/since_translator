# Быстрый старт: Установка OCR для Windows

## ✅ Что уже сделано

- Python библиотеки установлены: `pytesseract`, `pdf2image`, `Pillow`

## 📥 Что нужно установить

### 1. Tesseract OCR (обязательно)

1. **Скачайте установщик**:
   - Перейдите: https://github.com/UB-Mannheim/tesseract/wiki
   - Скачайте последнюю версию (например, `tesseract-ocr-w64-setup-5.x.x.exe`)

2. **Установите**:
   - Запустите установщик
   - Выберите стандартную папку: `C:\Program Files\Tesseract-OCR`
   - **ВАЖНО**: При выборе компонентов отметьте:
     - ✅ Russian
     - ✅ English  
     - ✅ Arabic
     - ✅ Chinese Simplified
   - Завершите установку

3. **Проверьте установку**:
   ```bash
   python check_tesseract.py
   ```

### 2. Poppler (для конвертации PDF в изображения)

1. **Скачайте**: https://github.com/oschwartz10612/poppler-windows/releases
   - Выберите последний релиз (например, `Release-23.11.0-0.zip`)

2. **Распакуйте** в `C:\poppler` (или другое место)

3. **Добавьте в PATH**:
   - Нажмите `Win + R`, введите `sysdm.cpl`, нажмите Enter
   - Вкладка "Дополнительно" → "Переменные среды"
   - В "Системные переменные" найдите `Path` → "Изменить"
   - "Создать" → добавьте путь к `bin` (например: `C:\poppler\Library\bin`)
   - OK → OK → OK

4. **Проверьте** (в новом терминале):
   ```bash
   pdftoppm -h
   ```

## 🚀 После установки

1. **Проверьте все компоненты**:
   ```bash
   python check_tesseract.py
   ```

2. **Пересоберите глоссарий**:
   ```bash
   python build_glossary.py
   ```

Теперь парсер сможет обрабатывать PDF с изображениями (например, "Часть 3.pdf")!

## ⚠️ Если что-то не работает

### Tesseract не найден
- Убедитесь, что установлен в `C:\Program Files\Tesseract-OCR`
- Перезапустите терминал после установки
- Запустите `python check_tesseract.py` для диагностики

### Poppler не найден
- Убедитесь, что добавили `bin` в PATH
- Перезапустите терминал
- Или укажите путь в коде (см. `INSTALL_OCR.md`)

### OCR работает медленно
- Это нормально: ~5-10 секунд на страницу
- Зависит от качества изображения и размера текста

## 📝 Альтернатива

Если установка вызывает проблемы, можно:
1. Использовать онлайн OCR сервисы
2. Конвертировать PDF в TXT вручную
3. Сохранить результат в `glossary/{language}/` как `.txt` файл
4. Пересобрать глоссарий


